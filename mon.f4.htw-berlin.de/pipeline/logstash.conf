
input {
	beats {
		port => 5044
	}

	tcp {
		port => 5000
	}
}

output { 
	stdout {} 
	
	elasticsearch {
		# TODO: move to enviroment variables
		hosts => "elasticsearch:9200"
		user => "elastic"
		password => "changeme"
		ecs_compatibility => disabled
	}
}
